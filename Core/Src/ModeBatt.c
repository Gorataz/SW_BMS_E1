#include "ModeBatt.h"

void ModeBatt_setMode(int mode)
{
	//do your thing mate
}


void ModeBatt_resetMode(void)
{
	//do your thing mate
}


int ModeBatt_checkModeBattery(void)
{
	int val;
	//do your thing mate
	/*
	 * Faire un while on reçoit pas un ACK (1) ou que l'on a pas encore dépassé les 100 essais
	 * Si on a dépassé les 100 essais sans avoir un ACK : on renvoit un message d'erreur et on essaye de renvoyer la commande
	 * La fonctionnalité précédente
	 */
	return val;
}
